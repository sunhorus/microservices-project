import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'udagram-frontend-new2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
